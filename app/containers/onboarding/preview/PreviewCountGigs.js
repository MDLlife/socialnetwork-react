import React, {Component} from 'react';
import PropTypes from 'prop-types';
import {connect} from 'react-redux';

class PreviewCountGigs extends Component {
    render() {
        return (
            <div>

            </div>
        )
    }
}

export default connect()(PreviewCountGigs);