# Default values for socialnetworkreact.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

socialnetworkreact:
  image: rg.fr-par.scw.cloud/comentarismoregistry/socialnetworkreact
  tag: latest
  # set to never when using local images
  pullPolicy: Always
  env:
    PORT: 80
    REDIS_HOST: 94.23.218.88
    REDIS_PASSWORD: wFft5VP4f84MCbWVq4cfHjUUeR#nFFpSVDdX4VWVZqqHfYKDKRs
    RETHINKDB_HOST: 94.23.218.88
    RETHINKDB_PASSWORD: 9jYqGM4udXkvwgs4CcrUGYlYYlNurKzsJPEwWaLM41o=
    OUTPUT_MODE: pretty
    NODE_ENV: production
    NODE_PATH: ./app

healthcheck:
  url: /health
  port: 80

service:
  type: NodePort
  port: 80
  externalPort: 80
  internalPort: 80

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
    nginx.ingress.kubernetes.io/rewrite-target: /
  path: /
  hosts:
    - www.mdl.cx
    - socialnetwork-react.cloud.comentarismo.com

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
